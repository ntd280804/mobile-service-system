@model WebApp.Models.Permission.UserProfileViewModel
@{
    ViewData["Title"] = "Quản lý Profile";
}

<div class="container mt-4">
    <h2 class="mb-4">👤 Quản lý Profile</h2>

    @if (TempData["CreateProfileMessage"] != null)
    {
        <div class="alert alert-info">@TempData["CreateProfileMessage"]</div>
    }
    @if (TempData["DeleteProfileMessage"] != null)
    {
        <div class="alert alert-warning">@TempData["DeleteProfileMessage"]</div>
    }
    @if (TempData["UpdateProfileMessage"] != null)
    {
        <div class="alert alert-info">@TempData["UpdateProfileMessage"]</div>
    }
    @if (TempData["AssignProfileMessage"] != null)
    {
        <div class="alert alert-success">@TempData["AssignProfileMessage"]</div>
    }
    @if (TempData["RevokeProfileMessage"] != null)
    {
        <div class="alert alert-danger">@TempData["RevokeProfileMessage"]</div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <div class="row">
        <div class="col-md-6 mb-4">
            <div class="card shadow">
                <div class="card-header bg-secondary text-white">
                    <strong>📜 Danh sách Profile</strong>
                </div>
                <div class="card-body p-0">
                    <div style="max-height: 300px; overflow-y: auto;">
                        <table class="table table-striped table-bordered mb-0">
                            <thead class="table-secondary">
                                <tr>
                                    <th>Tên Profile</th>
                                    <th>Idle Time</th>
                                    <th>Connect Time</th>
                                    <th>Failed Login</th>
                                    <th>Lock Time</th>
                                    <th>Inactive Account Time</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Profiles.Any())
                                {
                                    foreach (var profile in Model.Profiles)
                                    {
                                        <tr>
                                            <td>@profile.ProfileName</td>
                                            <td>@profile.IdleTime</td>
                                            <td>@profile.ConnectTime</td>
                                            <td>@profile.FailedLogin</td>
                                            <td>@profile.LockTime</td>
                                            <td>@profile.InactiveAccountTime</td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="6" class="text-center text-muted">Không có profile nào.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-6 mb-4">
            <div class="card shadow">
                <div class="card-header bg-dark text-white">
                    <strong>👥 User và Profile</strong>
                </div>
                <div class="card-body p-0">
                    <div style="max-height: 300px; overflow-y: auto;">
                        <table class="table table-striped table-bordered mb-0">
                            <thead class="table-secondary">
                                <tr>
                                    <th>Tên User</th>
                                    <th>Profile</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Users != null && Model.Users.Any())
                                {
                                    foreach (var user in Model.Users)
                                    {
                                        <tr>
                                            <td>@user.Username</td>
                                            <td>@user.Profile</td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="2" class="text-center text-muted">Không có user nào.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6 mb-4">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <strong>➕ Tạo Profile mới</strong>
                </div>
                <div class="card-body">
                    <form asp-action="CreateProfile" method="post">
                        <div class="form-group mb-3">
                            <label for="ProfileName">Tên Profile</label>
                            <input type="text" name="ProfileName" class="form-control" placeholder="Nhập tên profile" required />
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-2">
                                    <label for="IdleTime">Idle Time</label>
                                    <input type="text" name="IdleTime" class="form-control" value="UNLIMITED" />
                                </div>
                                <div class="form-group mb-2">
                                    <label for="ConnectTime">Connect Time</label>
                                    <input type="text" name="ConnectTime" class="form-control" value="UNLIMITED" />
                                </div>
                                <div class="form-group mb-2">
                                    <label for="FailedLogin">Failed Login Attempts</label>
                                    <input type="text" name="FailedLogin" class="form-control" value="UNLIMITED" />
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group mb-2">
                                    <label for="LockTime">Password Lock Time</label>
                                    <input type="text" name="LockTime" class="form-control" value="UNLIMITED" />
                                </div>
                                <div class="form-group mb-2">
                                    <label for="InactiveAccountTime">Inactive Account Time</label>
                                    <input type="text" name="InactiveAccountTime" class="form-control" value="UNLIMITED" />
                                </div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary mt-2">Tạo Profile</button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-6 mb-4">
            <div class="card shadow">
                <div class="card-header bg-danger text-white">
                    <strong>🗑️ Xóa Profile</strong>
                </div>
                <div class="card-body">
                    <form asp-action="DeleteProfile" method="post">
                        <div class="form-group mb-3">
                            <label for="profileName">Chọn Profile</label>
                            <select name="profileName" class="form-control" required>
                                <option value="">-- Chọn Profile --</option>
                                @foreach (var p in Model.Profiles)
                                {
                                    <option value="@p.ProfileName">@p.ProfileName</option>
                                }
                            </select>
                        </div>
                        <button type="submit" class="btn btn-danger">Xóa Profile</button>
                    </form>
                </div>
            </div>

            <div class="card shadow mt-4">
                <div class="card-header bg-warning text-dark">
                    <strong>✏️ Cập nhật Profile</strong>
                </div>
                <div class="card-body">
                    <form asp-action="UpdateProfile" method="post">
                        <div class="form-group mb-3">
                            <label for="ProfileName">Chọn Profile</label>
                            <select name="ProfileName" id="updateProfileName" class="form-control" required>
                                <option value="">-- Chọn Profile --</option>
                                @foreach (var p in Model.Profiles)
                                {
                                    <option value="@p.ProfileName">@p.ProfileName</option>
                                }
                            </select>
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-2">
                                    <label for="IdleTime">Idle Time (để trống nếu không đổi)</label>
                                    <input type="text" name="IdleTime" class="form-control" placeholder="UNLIMITED hoặc số" />
                                </div>
                                <div class="form-group mb-2">
                                    <label for="ConnectTime">Connect Time (để trống nếu không đổi)</label>
                                    <input type="text" name="ConnectTime" class="form-control" placeholder="UNLIMITED hoặc số" />
                                </div>
                                <div class="form-group mb-2">
                                    <label for="FailedLogin">Failed Login Attempts (để trống nếu không đổi)</label>
                                    <input type="text" name="FailedLogin" class="form-control" placeholder="UNLIMITED hoặc số" />
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group mb-2">
                                    <label for="LockTime">Password Lock Time (để trống nếu không đổi)</label>
                                    <input type="text" name="LockTime" class="form-control" placeholder="UNLIMITED hoặc số" />
                                </div>
                                <div class="form-group mb-2">
                                    <label for="InactiveAccountTime">Inactive Account Time (để trống nếu không đổi)</label>
                                    <input type="text" name="InactiveAccountTime" class="form-control" placeholder="UNLIMITED hoặc số" />
                                </div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-warning mt-2">Cập nhật Profile</button>
                    </form>
                </div>
            </div>

            <div class="card shadow mt-4">
                <div class="card-header bg-success text-white">
                    <strong>🔐 Gán Profile cho User</strong>
                </div>
                <div class="card-body">
                    <form asp-action="AssignProfile" method="post">
                        <div class="form-group mb-3">
                            <label for="userName">Chọn User</label>
                            <select name="userName" class="form-control" required>
                                <option value="">-- Chọn User --</option>
                                @foreach (var u in Model.Users)
                                {
                                    <option value="@u.Username">@u.Username</option>
                                }
                            </select>
                        </div>
                        <div class="form-group mb-3">
                            <label for="profileName">Chọn Profile</label>
                            <select name="profileName" class="form-control" required>
                                <option value="">-- Chọn Profile --</option>
                                @foreach (var p in Model.Profiles)
                                {
                                    <option value="@p.ProfileName">@p.ProfileName</option>
                                }
                            </select>
                        </div>
                        <button type="submit" class="btn btn-success">Gán Profile</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>